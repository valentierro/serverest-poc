{
  "name": "cypress-poc",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test:api:dev": "CYPRESS_ENV=dev cypress run --spec 'cypress/e2e/api/**/*.spec.ts'",
    "test:api:stg": "CYPRESS_ENV=stg cypress run --spec 'cypress/e2e/api/**/*.spec.ts'",
    "test:api:prod": "CYPRESS_ENV=prod cypress run --spec 'cypress/e2e/api/**/*.spec.ts'",
    "test:ui:dev": "CYPRESS_ENV=dev cypress run --spec 'cypress/e2e/ui/**/*.spec.ts'",
    "test:ui:stg": "CYPRESS_ENV=stg cypress run --spec 'cypress/e2e/ui/**/*.spec.ts'",
    "test:ui:prod": "CYPRESS_ENV=prod cypress run --spec 'cypress/e2e/ui/**/*.spec.ts'",
    "test:todos:dev": "CYPRESS_ENV=dev cypress run --spec 'cypress/e2e/**/*.spec.ts'",
    "test:todos:stg": "CYPRESS_ENV=stg cypress run --spec 'cypress/e2e/**/*.spec.ts'",
    "test:todos:prod": "CYPRESS_ENV=prod cypress run --spec 'cypress/e2e/**/*.spec.ts'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@faker-js/faker": "^10.1.0",
    "@types/node": "^24.9.2",
    "cypress": "^15.5.0",
    "typescript": "^5.9.3"
  }
}
